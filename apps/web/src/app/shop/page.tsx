import { ShopPage } from "@/views/ShopPage";

export const metadata = {
  title: "Shop | LightRain",
  description: "Cold wallets, Bitcoin security gear, and merch at shop.lightra.in.",
};

export default function Page() {
  return <ShopPage />;
}

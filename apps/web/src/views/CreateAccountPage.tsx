"use client";

import { SecurityOnboardingWizard } from "./SecurityOnboardingWizard";

/** Multi-step security onboarding for the private vault (see SecurityOnboardingWizard). */
export function CreateAccountPage() {
  return <SecurityOnboardingWizard />;
}
